<p> this will be a guide to virtual machine templates to use the system.</p>
<p>it will let you know what is needed to do with the virtual machine templates
  in order for the system to be able to communicate with the machines  </p>
  <p><b>needed programs</b></p>
  
<p>acpid (needed for virsh shutdown):<br/>
<i>sudo apt-get install acpid </i></p>
<p>Ruby for virtual machine feedback <br/>
<i>sudo apt-get install ruby</i></p>

<p><b>needed settings</b></p>
